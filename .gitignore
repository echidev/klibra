# FINDEX .gitignore
# Agents & Coding Vibe - ignore tool runtime, keep portfolio artifacts

# ── Secrets & Credentials (never commit) ──────────────────
.env
.env.*
.env.*.local
secrets.*
credentials.*
*.key
*.pem
*.p12
*.pfx

# ── Agents & Coding Vibe (tool runtime - not portfolio) ─────
.opencode/
.specify/
.codegraph/
.config/
opencode.jsonc
specs/
AGENTS.md
.vscode/
.idea/

# ── Dependencies ──────────────────────────────────────────
node_modules/

# ── Python ────────────────────────────────────────────────
venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.whl

# ── Infrastructure State (Terraform) ───────────────────────
*.tfstate
*.tfstate.*
*.tfvars
terraform.tfstate
terraform.tfvars
.terraform/
.terraform.lock.tfstate

# ── Logs & Runtime ────────────────────────────────────────
*.log
logs/
log/
__logs__/
airflow_logs/
dbt_logs/
transformation/dbt/target/
transformation/dbt/.user.yml

# ── Data & Cache ──────────────────────────────────────────
/data/
*.parquet
*.avro
*.csv
*.xlsx
*.json
!docs/**/*.json
*.db
*.sqlite
*.sqlite3
*.duckdb
.duckdb/
*.minio/
minio_data/

# ── Coverage & Test Artifacts ─────────────────────────────
coverage/
htmlcov/
.coverage
coverage.xml
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/

# ── Docker ────────────────────────────────────────────────
docker-compose.override.yml
.docker/

# ── CI/CD (keep templates, ignore generated) ──────────────
.github/workflows/*.generated.yml

# ── Documentation Build ───────────────────────────────────
_site/
site/
*.pdf
